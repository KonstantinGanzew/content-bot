# Парсер-бот для JoyReactor и Telegram

Модульный парсер-бот для автоматического парсинга картинок с JoyReactor и отправки их в Telegram группу. Архитектура позволяет легко добавлять новые источники контента.

## Возможности

- 🚀 Автоматический парсинг постов с JoyReactor
- 📱 Отправка изображений и видео в Telegram группу/канал
- 📥 **Умная обработка медиа**: скачивание → проверка → отправка → очистка
- 🎥 **Поддержка видео**: MP4, WebM, AVI, MOV, MKV, FLV
- 🎥 **Расширенный поиск видео**: находит видео в video тегах, ссылках, data-атрибутах и HTML тексте
- 🔍 **Умная обработка JoyReactor**: автоматический поиск .webm, .mp4 и других видео форматов
- 📱 **Чистая отправка медиа**: изображения и видео отправляются без подписей и текста
- 📸 **Множественные медиа**: все картинки и видео из одного поста отправляются в одном сообщении
- 🗄️ База данных для отслеживания отправленных постов
- ⚙️ Модульная архитектура для добавления новых ресурсов
- 📊 Логирование и мониторинг
- 🔄 Планировщик с настраиваемыми интервалами
- 🛡️ Защита от дублирования и проверка размеров/форматов
- 🔧 Умная система предотвращения дубликатов
- 🚫 Фильтрация нежелательного контента
- ✅ **Поддержка кириллических URL** - автоматическое кодирование URL с русскими символами
- 🚫 **Автоматическая фильтрация комментариев** - исключение контейнеров с `class="comment-content"`

## Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repo_url>
   cd parser-bot
   ```

2. **Установить зависимости** (если еще не установлены):
   ```bash
   pip install -r requirements.txt
   ```

3. **Создайте файл `.env` с настройками:**
   ```env
   # Настройки Telegram Bot
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   TELEGRAM_CHAT_ID=your_chat_id_here

   # Настройки парсинга
   PARSER_ENABLED=true
   PARSER_INTERVAL=300

   # Настройки логирования
   LOG_LEVEL=INFO

   # Путь к базе данных
   DATABASE_FILE=data/sent_posts.json
   ```

## Настройка Telegram Bot

1. **Создайте бота через @BotFather:**
   - Откройте Telegram и найдите @BotFather
   - Выполните команду `/newbot`
   - Следуйте инструкциям и получите токен

2. **Получите Chat ID:**
   - Добавьте бота в группу/канал
   - Отправьте сообщение в группу
   - Перейдите по ссылке: `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
   - Найдите `chat.id` в ответе

## Запуск

```bash
python main.py
```

## Обработка медиа файлов

Бот использует умную систему обработки изображений и видео:

### Процесс отправки:
1. **📊 Проверка** - получение размера и типа медиа файла
2. **📥 Скачивание** - сохранение во временную папку
3. **✅ Валидация** - проверка размера и формата  
4. **📤 Отправка** - загрузка в Telegram через локальный файл
5. **🗑️ Очистка** - автоматическое удаление временного файла

### Преимущества:
- ✅ Более стабильная отправка через локальные файлы
- ✅ Проверка медиа файлов до отправки
- ✅ Контроль размеров файлов (10MB изображения, 50MB видео)
- ✅ Автоматическая очистка временных файлов
- ✅ Лучшая обработка ошибок
- ✅ **Поддержка кириллических URL** - правильная обработка русских символов в именах файлов

### Поддерживаемые форматы:

**Изображения:**
- JPG/JPEG
- PNG  
- GIF
- WebP

**Видео:**
- MP4
- WebM
- AVI
- MOV
- MKV
- FLV

### Тестирование:
```bash
# Тест скачивания (без отправки в Telegram)
manual_parse.bat

# Проверка что найденные медиа файлы правильного типа
clear_db.bat && manual_parse.bat
```

## Предотвращение дубликатов

Бот использует многоуровневую систему защиты от дубликатов:

### 🔧 Система дедупликации:
1. **Улучшенная генерация ID** - комбинированные хеши (URL + заголовок + время)
2. **Фильтрация в парсере** - отслеживание ID и изображений в рамках сессии  
3. **Проверка базы данных** - двойная проверка перед отправкой
4. **Защита в планировщике** - дополнительные проверки в отправщике

### 📊 Диагностика дубликатов:
```bash
# Полная диагностика дубликатов
fix_duplicates.bat

# Тест системы исправлений
test_duplicates_fix.bat

# Диагностика в реальном времени
diagnose_dup.bat
```

### 🚫 Фильтрация контента:

Автоматическая фильтрация нежелательных постов:
- Посты с "отличный комментарий" 
- Другие настраиваемые фразы
- **Автоматическое исключение комментариев** с `class="comment-content"`
- Конфигурируется в `config/constants.py`

```python
# Управление фильтрацией
CONTENT_FILTERS = {
    'JOYREACTOR': {
        'FILTER_ENABLED': True,  # Вкл/выкл фильтрацию
        'BLOCKED_PHRASES': [
            'отличный комментарий',
            'прекрасный комментарий',
            # ... добавьте свои фразы
        ]
    }
}
```

**Автоматическая фильтрация комментариев:**
- Исключаются контейнеры содержащие `<div class="comment-content">`
- Логируется количество отфильтрованных комментариев
- Улучшает качество контента в Telegram канале

### 🛠️ Инструменты диагностики:
- `fix_duplicates.py` - анализ и исправление дубликатов
- `diagnose_duplicates.py` - детальная диагностика 
- `test_duplicates_fix.py` - тест системы исправлений
- `test_filter_content.py` - тест фильтрации контента

## 🚨 Решение проблемы дубликатов

**Проблема**: Бот отправляет одинаковые картинки/видео из разных постов  
**Причина**: Разные посты содержали одинаковые медиа файлы  
**Решение**: Отслеживание уникальности URL медиа + группировка множественных медиа

### 🔧 Быстрое исправление:
```bash
# Диагностика проблемы
diagnose_duplicates.bat

# Исправление дубликатов в базе
fix_duplicates.bat
```

### 📊 Что исправлено:
- **Уникальность URL**: Одинаковые картинки/видео отправляются только один раз
- **Группировка медиа**: Множественные медиа из поста отправляются одной группой
- **Проверка единственности**: Только один экземпляр бота может работать одновременно
- **Стабильные ID**: Убрана зависимость от времени в генерации ID постов
- **Двойная база данных**: Отслеживание постов И URL медиа отдельно

### 🎯 Новое поведение:
- **Разные посты с одинаковой картинкой** → картинка отправится только один раз
- **Пост с 3 уникальными медиа** → 1 группа из 3 медиа в Telegram
- **Пост только с уже отправленными медиа** → пост пропускается
- **Смешанный пост** → отправляются только новые медиа

### 💡 Диагностика:
```bash
# Полная диагностика
diagnose_duplicates.bat

# Проверка базы данных (посты + URL медиа)
python show_database.py

# Очистка всей базы (посты + URL медиа)
clear_db.bat
```

### 🗄️ Структура базы данных:
```json
{
  "sent_posts": ["joyreactor_123", "joyreactor_456"],
  "sent_media_urls": [
    "https://img.joyreactor.cc/pic1.jpg",
    "https://img.joyreactor.cc/video1.webm"
  ]
}
```

## 📱 Функции бота

## Структура проекта

```
parser-bot/
├── config/                 # Конфигурация и константы
│   ├── constants.py        # Константы для различных ресурсов
│   └── settings.py         # Настройки из переменных окружения
├── parsers/                # Парсеры для различных ресурсов
│   ├── base_parser.py      # Базовый класс парсера
│   └── joyreactor_parser.py # Парсер для JoyReactor
├── telegram/               # Модуль для работы с Telegram
│   └── bot.py             # Отправка сообщений в Telegram
├── utils/                  # Утилиты
│   ├── database.py        # Работа с базой данных постов
│   └── logger.py          # Настройка логирования
├── data/                   # Данные (создается автоматически)
├── logs/                   # Логи (создается автоматически)
├── main.py                # Точка входа
├── scheduler.py           # Планировщик задач
└── requirements.txt       # Зависимости
```

## Добавление новых ресурсов

Для добавления нового ресурса:

1. **Добавьте константы в `config/constants.py`:**
   ```python
   PARSERS = {
       'JOYREACTOR': {...},
       'NEW_RESOURCE': {
           'BASE_URL': 'https://new-resource.com',
           'TAG_URL': 'https://new-resource.com/category',
           'USER_AGENT': '...',
           'REQUEST_DELAY': 2,
           'IMAGE_SELECTORS': {
               'POST_CONTAINER': '.post',
               'IMAGE': 'img',
               'POST_LINK': 'a[href*="/post/"]'
           }
       }
   }
   ```

2. **Создайте парсер в `parsers/`:**
   ```python
   from .base_parser import BaseParser, PostData
   from config.constants import PARSERS
   
   class NewResourceParser(BaseParser):
       def __init__(self):
           config = PARSERS['NEW_RESOURCE']
           super().__init__(config['BASE_URL'])
           # Реализуйте методы parse_posts и get_post_details
   ```

3. **Добавьте парсер в `scheduler.py`:**
   ```python
   self.parsers = {
       'joyreactor': JoyReactorParser(),
       'new_resource': NewResourceParser()
   }
   ```

## Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | **Обязательно** |
| `TELEGRAM_CHAT_ID` | ID чата/группы | **Обязательно** |
| `PARSER_ENABLED` | Включить парсинг | `true` |
| `PARSER_INTERVAL` | Интервал парсинга (сек) | `300` |
| `LOG_LEVEL` | Уровень логирования | `INFO`